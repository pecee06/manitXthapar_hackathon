const Prediction = ({ styles }) => {
	return (
		<div
			className={`${styles}  flex flex-col justify-center items-center overflow-y-scroll`}
		>
			Prediction
		</div>
	);
};

export default Prediction;
